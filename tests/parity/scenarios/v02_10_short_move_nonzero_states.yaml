name: v0.2 short move with nonzero start and target states
n_dofs: 1
current_velocity: [0.3]
current_acceleration: [0.5]
target_position: [0.2]
target_velocity: [0.2]
target_acceleration: [0.2]
max_velocity: [2.0]
max_acceleration: [5.0]
max_jerk: [10.0]
cycle_time: 0.010
# Parity at the floating-point floor (~1e-15) on every cycle, including the
# finishing one: past `duration` both Ruckig and the SCL FB extrapolate along
# the moving final state (jerk=0, a stays = aT), so no terminal-sample override
# is needed.
